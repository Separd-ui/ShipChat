<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_alignParentEnd="true"
    android:layout_margin="5dp"
    android:textAlignment="textEnd">

    <TextView
        android:id="@+id/right_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:autoText="false"
        android:background="@drawable/back_right"
        android:foregroundGravity="right"
        android:gravity="end"
        android:inputType="textMultiLine"
        android:padding="8dp"
        android:text="text"
        android:textAllCaps="false"
        android:textColor="@color/white"
        app:layout_constrainedWidth="true"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="1.0"
        app:layout_constraintStart_toEndOf="@+id/right_time"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/right_seen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/seen"
        android:visibility="visible"
        app:layout_constraintEnd_toEndOf="@+id/right_text"
        app:layout_constraintTop_toBottomOf="@+id/right_text" />

    <TextView
        android:id="@+id/right_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="TextView"
        app:layout_constraintBottom_toBottomOf="@+id/right_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/right_text" />
</androidx.constraintlayout.widget.ConstraintLayout>